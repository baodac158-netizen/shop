<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="examplemybatis.mapper.CartMapper">

    <!-- 查询某用户购物车列表 -->
    <select id="listByUserId" resultType="examplemybatis.pojo.CartItem">
        SELECT id, user_id AS userId, product_id AS productId, quantity, price
        FROM cartitem
        WHERE user_id = #{userId}
        ORDER BY id
    </select>

    <!-- 插入购物车项 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cartitem (user_id, product_id, quantity, price)
        VALUES (#{userId}, #{productId}, #{quantity}, #{price})
    </insert>

    <!-- 更新购物车数量 -->
    <update id="updateQuantity">
        UPDATE cartitem
        SET quantity = #{quantity}
        WHERE id = #{id}
    </update>

    <!-- 删除购物车项 -->
    <delete id="delete">
        DELETE FROM cartitem WHERE id = #{id}
    </delete>

</mapper>
